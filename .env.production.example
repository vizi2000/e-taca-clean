# e-Taca Production Environment Configuration
# Copy this file to .env.production and fill in your values

# =============================================================================
# DATABASE
# =============================================================================
DB_USER=etaca
DB_PASSWORD=your_secure_password_here
DB_NAME=etaca_db

# =============================================================================
# SECURITY
# =============================================================================
# CRITICAL: Generate a unique secret for each environment
# Generate with: openssl rand -base64 64 | tr -d '\n'
# Minimum length: 64 characters
# NEVER reuse secrets across environments or commit actual secrets
JWT_SECRET=CHANGE_THIS_GENERATE_NEW_SECRET_WITH_OPENSSL_COMMAND_ABOVE

# =============================================================================
# APPLICATION
# =============================================================================
ASPNETCORE_ENVIRONMENT=Production
FRONTEND_URL=https://e-taca.borg.tools
FRONTEND_PORT=3007
NEXT_PUBLIC_API_URL=/api

# =============================================================================
# PAYMENT GATEWAY (Fiserv)
# =============================================================================
# Contact Fiserv for production credentials
# NEVER commit actual credentials to the repository
FISERV_STORE_ID=your_fiserv_store_id_here
FISERV_SECRET=your_fiserv_secret_here

# =============================================================================
# EMAIL (Optional)
# =============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_ADDRESS=noreply@e-taca.borg.tools
SMTP_FROM_NAME=e-Taca